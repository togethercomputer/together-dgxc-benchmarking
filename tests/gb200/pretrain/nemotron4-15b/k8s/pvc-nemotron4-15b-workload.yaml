apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nemotron4-15b-workload
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: vastdata-filesystem-tcp
  resources:
    requests:
      storage: 10Gi
